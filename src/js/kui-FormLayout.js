/**
 * 格式化表单对象定义。
 * 
 * @param {object} opts
 *        配置项，包括：
 *        <ul>
 *          <li></li>
 *        </ul> 
 */
function FormLayout(opts) {
  var fields = opts.fields || [];
  var idField = opts.idField;
  var loadUrl = opts.loadUrl;
  for (var i = 0; i < opts.fields.length; i++) {

  }
}

FormLayout.prototype.render = function(containerId) {
  
};

FormLayout.prototype.getData = function() {

};

FormLayout.prototype.observe = function(message, callback) {
  
};